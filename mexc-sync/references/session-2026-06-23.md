# MEXC API — Session 2026-06-23 Key Findings

## Verified API Flow (06:45 UTC)

| Test | Method | URL | Result |
|------|--------|-----|--------|
| GET public | proxy | mexc-proxy...workers.dev/api/v1/contract/detail | ✅ 925 pairs |
| GET public | direct | api.mexc.com/api/v1/contract/detail | ✅ 925 pairs |
| GET private (balance) | proxy | mexc-proxy...workers.dev/api/v1/private/account/assets | ✅ USDT 20.43 |
| GET private (balance) | direct | api.mexc.com/api/v1/private/account/assets | ✅ USDT 20.43 |
| POST order | proxy | mexc-proxy...workers.dev/api/v1/private/order/submit | ❌ 403 Forbidden |
| POST order | direct | api.mexc.com/api/v1/private/order/submit | ✅ code=0 |
| GET positions | direct | api.mexc.com/api/v1/private/position/open_positions | ✅ 1 position |

## Key Rules
1. GET → proxy or direct both work
2. POST → MUST be direct (proxy = 403)
3. Auth → Headers (ApiKey, Request-Time, Signature), NOT query params
4. Signature → HMAC-SHA256(secret, api_key + timestamp_ms + body_json)
5. curl_cffi → broken on PRoot/Android (libm.so: invalid ELF header)
6. Use urllib.request instead of curl_cffi in MEXCClient

## Bot MEXCClient Fix Applied
- Replaced curl_cffi.requests with urllib.request
- POST always goes to https://api.mexc.com (hardcoded, not via proxy)
- GET goes via self.base_url (proxy)
- Added ssl.create_default_context() with verify_mode = CERT_NONE

## Ahmad Agent Original Setup (Replaced 2026-06-23)
- File: predator_v5.py (1924 lines, 18 classes, 7 upgrade modules)
- Injected: CF Worker URL, API keys from secrets, capital $21.42, leverage 30x, dry_run=False
- Bot running LIVE, scanning every 3s
- Status: $21.37, no trades yet (Asia dead zone)

## Session Filter Behavior
- UPGRADES_LOADED = False (curl_cffi broken) → session filter skip NOT active
- Scanner applies -2 score penalty for off-hours (not hard block)
- min_score=8 (Ahmad default) → hard to reach in Asia session low volatility
- Bot scans but doesn't find score 8+ signals in dead zone → normal behavior
